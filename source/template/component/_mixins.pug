mixin nav(className, items, activeItem)
	nav(class=`${className}`)
		.container
			ul(class=`${className}__list`)
				each item, ndx in items
					li(class=`${className}__item` + ( (ndx === activeItem) ? ` ${className}__item_active` : '') )
						a(class=`${className}__link` href='#') #{item}

mixin social(className, items)
	ul(class=`${className}`)
		each item in items
			li(class=`${className}-item`)
				a(class=`${className}-item-link` href='')
					i(class=`fa ` + `fa-${item}`)